%h2 Applications

%h3 Received
%ul
  - @got.each do |cv, job|
    %li
      = h cv.user.public_name
      applied with
      = cv.href(cv.title, :read)
      for
      = job.href(job.title, :read)
%h3 Sent
%ul
  - @sent.each do |job, cv|
    %li
      You applied with
      = cv.href(cv.title, :read)
      for
      = job.href(job.title, :read)
