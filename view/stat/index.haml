%h2 Statistics

= pager_navigation

- if @pager.empty?
  %p No Statistics collected yet.
- @pager.each do |stat|
  - if view = stat.job_view_stat
    = render_partial(:view, Stats, :stat => stat, :view => view)
  - if search = stat.search_stat
    = render_partial(:search, Stats, :stat => stat, :search => search)

= pager_navigation
